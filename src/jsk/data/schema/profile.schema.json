{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "jsk region profile",
  "description": "What jsk.resume.build reads of a market's conventions, and nothing else. The forbidden/expected/private-field lists went with the URS record (2026-09-25): kb.ttl holds none of the fields they governed.",
  "type": "object",
  "required": ["id", "region", "pages", "sections", "work_rights"],
  "properties": {
    "$schema": { "type": "string" },
    "id": { "type": "string", "pattern": "^urs:profile:[a-z]{2}/\\d+$" },
    "region": { "type": "string", "pattern": "^([A-Z]{2}|XX)$", "description": "ISO 3166-1 alpha-2, or XX for the region-neutral default. Picks the paper size." },
    "label": { "type": "string" },
    "pages": { "type": "integer", "minimum": 1, "description": "The page budget when the resume.json names none." },
    "sections": {
      "type": "array",
      "items": { "enum": ["summary", "skills", "experience", "education", "credentials", "languages"] },
      "description": "The sections rendered, in this order."
    },
    "declaration": { "type": "boolean", "description": "The Indian declaration block with place and date." },
    "work_rights": { "type": "boolean", "description": "Put the right to work on the header line." }
  },
  "additionalProperties": false
}
