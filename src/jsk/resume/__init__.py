"""The resume, built from the career.

An application's resume.json names what was chosen for one posting - which bullets, in
what order, which skills, a summary, how to print it - and nothing the career already
holds. `short` reads and checks it; `build` turns it and career/kb.ttl into the render
plan the emitters take.
"""
